spring.application.name=AwanaBetania
server.address=0.0.0.0
server.port=8080

# Baza de date
spring.datasource.url=jdbc:mysql://localhost:3306/awana_club_manager
#spring.datasource.username=awana
#spring.datasource.password=1-Vxk2e7m3!@

spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Token pentru securizarea endpoint-urilor NFC
nfc.token=${NFC_TOKEN}

# Cheie criptare AES parole
aes.secret.key=${AES_SECRET_KEY}

# PIN acces Control Center admin
admin.pin=${ADMIN_PIN}

